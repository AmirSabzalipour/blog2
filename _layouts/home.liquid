---
layout: default
---

<div class="home">
    <!-- Header image -->
    <style scoped>
        {% assign image = site.header_feature_image | relative_url %}
        .call-out_img {
            background-image: url('{{ image }}');
        }
        {% if site.header_feature_image_responsive %}
        @media screen and (max-width: 768px) {
            .call-out_img {
                background-image: url('{{ image | append: '-medium' }}.{{ image | split: '.' | last }}');
            }
        }
        @media screen and (max-width: 576px) {
            .call-out_img {
                background-image: url('{{ image | append: '-small' }}.{{ image | split: '.' | last }}');
            }
        }
        {% endif %}
    </style>
    <div id="main" class="call-out call-out_img">
        <!-- Header text -->
        <h1>{{ site.header_text | default: "" }}</h1>
        <!-- Additional content -->
        <div class="additional-content">
            <h2>Welcome to My Blog</h2>
            <p>This is additional content you want to display on your homepage.</p>
        </div>
    </div>

    <!-- Recent posts -->
    {% capture _blog %}{% include blog/blog.liquid %}{% endcapture %}
    {{ _blog | split: " " | join: " "}}
</div>
